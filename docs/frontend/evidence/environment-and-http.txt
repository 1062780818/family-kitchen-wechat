FK-C02-001 环境与 HTTP 探测记录
审查日期：2026-09-03，Asia/Shanghai
仓库：https://github.com/llgululu/family-kitchen.git
基线分支：main
基线提交：572f3e20fb2c2ae243013c0bdc3e05054c708745
审查分支：codex/02-frontend-audit
环境：Windows，PowerShell，git 2.53.0.windows.3，Node v24.19.0
仓库声明：.nvmrc=20；packageManager=pnpm@9.12.0；mini-app=0.1.0
工具适配：默认 pnpm.cmd 为 11.19.0，实际成功安装和构建使用 pnpm.cmd dlx pnpm@9.12.0。

获取过程（终端输出摘录；不是完整会话日志）：
1. 默认 git clone 失败：git: 'remote-https' is not a git command.
2. 进程级设置 GIT_EXEC_PATH 到运行时 dependencies/native/git/mingw64/bin，未更改仓库文件。
3. 沙箱网络连接 github.com:443 失败；经工具授权后 clone 成功。
4. 默认 pnpm 11 在受限网络下 GET registry.npmjs.org 报 EACCES；停止该进程。
5. pnpm 9.12.0 按原锁文件筛选 mini-app... 安装，ignore-scripts，973 个包，Done in 33.3s，退出码 0。
6. 默认 pnpm 11 run 试图自动重新安装，因无 TTY 中止；没有采用强制清理，改用 pnpm 9.12.0。
7. pnpm dlx 在沙箱内报 EPERM realpath C:\Users\Administrator；经工具授权后原构建成功。
8. 未升级依赖，未修改 package.json、锁文件、业务代码、数据库、环境配置或 AppID。

HTTP 实测（原 H5 开发服务，--host 127.0.0.1）：
http://127.0.0.1:5173/ HTTP 200 length=859
http://127.0.0.1:5173/src/main.js HTTP 200 length=2206
http://127.0.0.1:5173/src/pages/tabbar/home/home.vue HTTP 200 length=116054
http://127.0.0.1:3000/api/v1/config/public FAILED 由于目标计算机积极拒绝，无法连接。 (127.0.0.1:3000)
说明：仅 HTTP/开发编译验证，不是浏览器交互、微信模拟器或真机验收。
小程序和 H5 开发服务验证后均已通过 Ctrl+C 停止，停止退出码 1 不代表编译失败。

pnpm-lock.yaml SHA256：27DFD7FA85EC2F7F532ED5778489B5F4B1F23C31FE6665BBB6FCCDE44A71057D
git diff 基线 -- mini-app backend admin packages package.json pnpm-lock.yaml：无差异，退出码 0。
已检查的 PATH 和常见 Tencent 安装位置没有发现微信开发者工具命令；未声称已遍历整台电脑。
manifest 的 mp-weixin.appid=your-appid，urlCheck=false。
小程序生产输出 app.json 注册 31 页；tabBar.custom=true，但产物无 custom-tab-bar 目录，需 Codex-03 真机复核。
