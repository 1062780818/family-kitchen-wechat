# C01→C03｜FK-C01-005独立复验交接

## 固定输入

- 分支：`codex/01-backend-foundation-fixes`
- 固定提交：以本文件所在最终提交ID为准（C01最终短卡提供）。不要按工作区浮动HEAD验收。
- 需求：`docs/codex/decisions/FK_CONFIRMED_REQUIREMENTS_V0.3.md`
- 实现报告：`docs/codex/reports/C01_FK-C01-005_REPORT.md`

## 复验命令与预期

使用pnpm 9.12.0，执行`pnpm run verify:retained`。预期：shared Lint／类型／构建；backend保留域Lint／类型／构建和7套件74测试；mini-app核心API Lint及微信构建全部成功。小程序类型检查和自动化测试不在通过项。

另执行全后端Jest和小程序全量Lint，预期仍可能失败；不得把暂缓模块失败混入保留域PASS，也不得删除失败证据。C01最近证据为后端21/23套件、139/158用例通过；小程序全量Lint 12错误、1460警告。

## 必验安全与并发矩阵

1. 匿名只可读`system/*`，家庭图片匿名GET失败；同家庭鉴权签名读成功，跨家庭、`../`、伪造familyId拒绝。
2. 上传正式菜图片仅丈夫／`creator`成功；妻子失败。本人删除成功，同家庭非上传者和跨家庭删除失败。
3. 妻子可浏览／收藏但不可新增、修改、下架正式菜；跨家庭菜谱不可读写。
4. 厨师状态接口拒绝妻子；妻子只可直接取消待确认单，已接单需申请且现阶段不得直接改变餐单。
5. 两个请求基于同一旧状态竞争时仅一个更新成功，另一个得到`ORDER_VERSION_CONFLICT`。

## 集成条件与未验证

若有隔离MySQL＋MinIO，可补真实签名URL、全桶策略替换和对象删除测试，只能用虚构图片。微信登录、订阅通知、真机、日期餐次唯一、亲亲价格快照、调整申请／确认尚无正式实现或环境，不得判为通过。C03应独立出具结论；本交接文件不等于已启动复验。
