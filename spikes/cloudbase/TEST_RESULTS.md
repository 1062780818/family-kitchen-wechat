# CloudBase隔离小样测试结果

- 日期：2026-09-07，Asia/Shanghai。
- 环境：Windows；Node v24.19.0。
- 命令：`node --test spikes/cloudbase/test/core.test.mjs`
- 结果：退出码0；13项通过、0项失败、0项跳过。

通过项：可信服务端身份来源；匿名／伪造身份拒绝；同家庭写入与跨家庭拒绝；同幂等键同载荷重放；同键不同载荷冲突；10次并发重试只生成一个本地结果；测试图片仅同家庭读取；删除仅允许测试图片所有者；数据库／存储客户端规则草案均默认拒绝；预约窗口包含今天与第7天且拒绝窗口外日期；订单重复请求返回同一餐单；同家庭／日期／餐次的不同请求返回已有进行中餐单；上菜后释放槽位；厨师不能伪造点菜者下单。

限制：并发测试使用单进程内存原子仓库，不能证明CloudBase跨实例事务；图片内容为虚构Buffer，未上传云端；安全规则仅解析并检查预期值，未部署；真实微信身份、云函数、数据库和存储均未调用。
